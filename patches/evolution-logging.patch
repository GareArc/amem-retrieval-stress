--- a/agentic_memory/memory_system.py
+++ b/agentic_memory/memory_system.py
@@ -706,7 +706,8 @@ class AgenticMemorySystem:
                 )

                 response_json = json.loads(response)
                 should_evolve = response_json["should_evolve"]
-
+                logger.info(f"Evolution decision for '{note.content[:60]}': should_evolve={should_evolve}, actions={response_json.get('actions', [])}, connections={response_json.get('suggested_connections', [])}")
+
                 if should_evolve:
                     actions = response_json["actions"]
                     for action in actions:
